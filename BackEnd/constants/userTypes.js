/**
 * User role constants used across the application.
 * These values must match the roles used in the frontend.
 */
export const USER_TYPES = Object.freeze({
  PATIENT: 'Patient',
  DOCTOR: 'Doctor',
  ADMIN: 'Admin',
});
